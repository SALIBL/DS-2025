
## 📊 Analyse Machine Learning – Global Health, Nutrition, Mortality, Economic Data

### *Compte rendu complet de toutes les étapes d’analyse et de modélisation*

---

# ## 1. 🎯 Introduction

Le projet porte sur l’analyse du dataset **Global Health, Nutrition, Mortality, Economic Data**, un ensemble de données mondial comprenant :

* l’espérance de vie,
* la nutrition,
* la mortalité,
* des indicateurs démographiques,
* des variables économiques,
* des caractéristiques par pays, par année et par genre.

L’objectif principal :
➡️ **explorer le dataset**,
➡️ **préparer les données**,
➡️ **détecter anomalies & valeurs manquantes**,
➡️ **produire des visualisations**,
➡️ **construire des modèles prédictifs (régression linéaire & logistique)**,
➡️ **évaluer les performances (matrice de confusion, ROC)**.

---

# ## 2. 📥 Importation des librairies

Les librairies suivantes sont utilisées :

* **pandas, numpy** : manipulation de données
* **matplotlib, seaborn** : visualisation
* **sklearn** : prétraitement, imputation, modèles, métriques
* **TSNE, Isomap** : réduction dimensionnelle
* **pickle** : sauvegarde des modèles
* **warnings** : filtrage des alertes

---

# ## 3. 📂 Chargement du dataset

Le fichier provient de Kaggle via **kagglehub** :

```python
path = kagglehub.dataset_download("miguelroca/global-health-nutrition-mortality-economic-data")
df = pd.read_csv(os.path.join(path, csv_file))
```

Visualisation initiale :

```python
df.info()
df.describe()
```

---

# ## 4. 🔎 Analyse exploratoire (EDA)

### ### 4.1 Distribution de l’Espérance de Vie

Plusieurs graphiques illustrent la distribution :

* Histogramme général
* Densités pour différents millésimes (1990, 2000, 2010, 2019)
* Boxplots par année

```python
sns.histplot(df['Life Expectancy'])
sns.distplot(df['Life Expectancy'][df['Year']==1990], ...)
plt.figure(figsize=(15,5))
sns.boxplot(x='Year', y='Life Expectancy', data=df)
```

### ### 4.2 Comparaison par genre

```python
sns.pairplot(df[['Year','Gender','Life Expectancy']], hue='Gender')
```

### ### 4.3 Détection des valeurs aberrantes (IQR)

```python
outliers_LifeExpectancy = find_outliers_IQR(df['Life Expectancy'])
sns.distplot(outliers_LifeExpectancy)
```

---

# ## 5. 🧹 Analyse & gestion des valeurs manquantes

Trois fonctions ont été créées :

### • Nombre de NaNs par ligne

```python
def nans_per_row(df): ...
```

### • Nombre de NaNs par pays

```python
def nans_per_country(df): ...
```

### • Histogramme des lignes contenant des NaNs

```python
def histNaNs(df): ...
```

Ces outils permettent d’évaluer la qualité des données avant modélisation.

---

# ## 6. 🌍 Visualisation par pays

La fonction `countriesGraph` trace l’évolution d’une variable (ex : espérance de vie) pour plusieurs pays :

```python
countriesGraph(df, ['Morocco', 'France'], 'Life Expectancy')
```

---

# ## 7. 📈 Modélisation Machine Learning

## ### 7.1 Régression Linéaire

La régression linéaire sert à prédire une variable **continue** — typiquement l’espérance de vie.

Exemple (présent implicitement dans le workflow) :

```python
from sklearn.linear_model import LinearRegression
model_lin = LinearRegression()
model_lin.fit(X_train, y_train)
y_pred = model_lin.predict(X_test)
```

Les métriques possibles incluent :

* R²
* MAE
* RMSE

---

## ### 7.2 Régression Logistique

Utilisée pour prédire une **classe binaire**, par exemple :

> « Espérance de vie élevée » (1) vs « faible » (0)

```python
from sklearn.linear_model import LogisticRegression
model_logistic = LogisticRegression()
model_logistic.fit(X_train_logistic, y_train_logistic)
```

---

# ## 8. 🧪 Évaluation du modèle de régression logistique

### ### 8.1 Matrice de confusion

```python
cm = confusion_matrix(y_test_logistic, y_pred_logistic)
sns.heatmap(cm, annot=True, fmt='d', cmap='Blues')
```

Cette matrice met en évidence :

* les vrais positifs (VP)
* les vrais négatifs (VN)
* les faux positifs (FP)
* les faux négatifs (FN)

---

### ### 8.2 Courbe ROC & AUC

```python
fpr, tpr, thresholds = roc_curve(y_test_logistic, y_pred_proba_logistic)
auc = roc_auc_score(y_test_logistic, y_pred_proba_logistic)
```

Visualisation :

* comparaison au classifieur aléatoire
* qualité de séparation du modèle
* surface sous la courbe (AUC)

---

# ## 9. 🗺️ Réduction dimensionnelle

Le fichier met à disposition deux techniques :

* **TSNE**
* **Isomap**

Ces méthodes servent à projeter les données en 2D pour révéler :
✔ clusters naturels
✔ similarités entre pays
✔ structure cachée du dataset

---

# ## 10. 💾 Sauvegarde des modèles

Possible via :

```python
pickle.dump(model_logistic, open('model_logistic.pkl','wb'))
pickle.dump(model_lin, open('model_lin.pkl','wb'))
```

---

# ## 11. 🏁 Conclusion générale

L’exploration complète du dataset Global Health, Nutrition, Mortality, Economic Data a permis :

### ✔ Une compréhension approfondie des tendances mondiales

— évolution de l’espérance de vie
— différences par genre et pays
— détection d’anomalies

### ✔ Une préparation du dataset

— gestion des NaNs
— traitement des valeurs extrêmes
— visualisations adaptées

### ✔ La construction de modèles de Machine Learning

— régression linéaire (prédiction continue)
— régression logistique (classification)

### ✔ Une évaluation rigoureuse

— matrice de confusion
— courbe ROC et AUC

