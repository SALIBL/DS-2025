
## NOM:SALIMA BLALET 
## FILIÈRE :CAC
## GROUPE :1
## TITRE:  global_health,_nutrition,_mortality,_economic_data

# 📊 Analyse Machine Learning – Global Health, Nutrition, Mortality, Economic Data

Compte rendu complet de toutes les étapes d’analyse et de modélisation

## 1. 🎯 Introduction

Le projet porte sur l’analyse du dataset Global Health, Nutrition, Mortality, Economic Data, un ensemble de données mondial comprenant :
l’espérance de vie,
la nutrition,
la mortalité,
des indicateurs démographiques,
des variables économiques,
des caractéristiques par pays, par année et par genre.
L’objectif principal :
➡️ explorer le dataset,
➡️ préparer les données,
➡️ détecter anomalies \& valeurs manquantes,
➡️ produire des visualisations,
➡️ construire des modèles prédictifs (régression linéaire \& logistique),
➡️ évaluer les performances (matrice de confusion, ROC).

## 2. 📥 Importation des librairies

Les librairies suivantes sont utilisées :
pandas, numpy : manipulation de données
matplotlib, seaborn : visualisation
sklearn : prétraitement, imputation, modèles, métriques
TSNE, Isomap : réduction dimensionnelle
pickle : sauvegarde des modèles
warnings : filtrage des alertes

## 3. 📂 Chargement du dataset

Le fichier provient de Kaggle via kagglehub :
path = kagglehub.dataset_download("miguelroca/global-health-nutrition-mortality-economic-data")
df = pd.read_csv(os.path.join(path, csv_file))

Visualisation initiale :
df.info()
df.describe()

## 4. 🔎 Analyse exploratoire (EDA)

### 4.1 Distribution de l’Espérance de Vie

Plusieurs graphiques illustrent la distribution :
Histogramme général
Densités pour différents millésimes (1990, 2000, 2010, 2019)
Boxplots par année
sns.histplot(df['Life Expectancy'])
sns.distplot(df['Life Expectancy'][df['Year']==1990], ...)
plt.figure(figsize=(15,5))
sns.boxplot(x='Year', y='Life Expectancy', data=df)

### 4.2 Comparaison par genre

sns.pairplot(df[['Year','Gender','Life Expectancy']], hue='Gender')

### 4.3 Détection des valeurs aberrantes (IQR)

outliers_LifeExpectancy = find_outliers_IQR(df['Life Expectancy'])
sns.distplot(outliers_LifeExpectancy)

## 5. 🧹 Analyse \& gestion des valeurs manquantes

Trois fonctions ont été créées :
• Nombre de NaNs par ligne
def nans_per_row(df): ...

• Nombre de NaNs par pays
def nans_per_country(df): ...

• Histogramme des lignes contenant des NaNs
def histNaNs(df): ...

Ces outils permettent d’évaluer la qualité des données avant modélisation.

## 6. 🌍 Visualisation par pays

La fonction countriesGraph trace l’évolution d’une variable (ex : espérance de vie) pour plusieurs pays :
countriesGraph(df, ['Morocco', 'France'], 'Life Expectancy')

## 7. 📈 Modélisation Machine Learning

### 7.1 Régression Linéaire

La régression linéaire sert à prédire une variable continue — typiquement l’espérance de vie.
Exemple (présent implicitement dans le workflow) :
from sklearn.linear_model import LinearRegression
model_lin = LinearRegression()
model_lin.fit(X_train, y_train)
y_pred = model_lin.predict(X_test)

Les métriques possibles incluent :
R²
MAE
RMSE

### 7.2 Régression Logistique

Utilisée pour prédire une classe binaire, par exemple :
« Espérance de vie élevée » (1) vs « faible » (0)
from sklearn.linear_model import LogisticRegression
model_logistic = LogisticRegression()
model_logistic.fit(X_train_logistic, y_train_logistic)

## 8. 🧪 Évaluation du modèle de régression logistique

### 8.1 Matrice de confusion

cm = confusion_matrix(y_test_logistic, y_pred_logistic)
sns.heatmap(cm, annot=True, fmt='d', cmap='Blues')

Cette matrice met en évidence :
les vrais positifs (VP)
les vrais négatifs (VN)
les faux positifs (FP)
les faux négatifs (FN)

### 8.2 Courbe ROC \& AUC

fpr, tpr, thresholds = roc_curve(y_test_logistic, y_pred_proba_logistic)
auc = roc_auc_score(y_test_logistic, y_pred_proba_logistic)

Visualisation :
comparaison au classifieur aléatoire
qualité de séparation du modèle
surface sous la courbe (AUC)

## 9. 🗺️ Réduction dimensionnelle

Le fichier met à disposition deux techniques :
TSNE
Isomap
Ces méthodes servent à projeter les données en 2D pour révéler :
✔ clusters naturels
✔ similarités entre pays
✔ structure cachée du dataset

## 10. 💾 Sauvegarde des modèles

Possible via :
pickle.dump(model_logistic, open('model_logistic.pkl','wb'))
pickle.dump(model_lin, open('model_lin.pkl','wb'))

## 11. 🏁 Conclusion générale

L’exploration complète du dataset Global Health, Nutrition, Mortality, Economic Data a permis :
✔ Une compréhension approfondie des tendances mondiales
— évolution de l’espérance de vie
— différences par genre et pays
— détection d’anomalies
✔ Une préparation du dataset
— gestion des NaNs
— traitement des valeurs extrêmes
— visualisations adaptées
✔ La construction de modèles de Machine Learning
— régression linéaire (prédiction continue)
— régression logistique (classification)
✔ Une évaluation rigoureuse
— matrice de confusion
— courbe ROC et AUC
Ce pipeline constitue une base complète et robuste pour un projet de Data Science appliqué à la santé mondiale.
donne moi ce rapport sous format markdwon

Voici ton rapport, proprement formaté en Markdown et prêt à être copié dans ton document.

***

# 📊 Analyse Machine Learning – Global Health, Nutrition, Mortality, Economic Data

Compte rendu complet de toutes les étapes d’analyse et de modélisation

## 1. 🎯 Introduction

Le projet porte sur l’analyse du dataset **Global Health, Nutrition, Mortality, Economic Data**, un ensemble de données mondial comprenant notamment :

- l’espérance de vie
- la nutrition
- la mortalité
- des indicateurs démographiques
- des variables économiques
- des caractéristiques par pays, par année et par genre

Les objectifs principaux sont :

- explorer le dataset
- préparer les données
- détecter les anomalies et valeurs manquantes
- produire des visualisations
- construire des modèles prédictifs (régression linéaire \& régression logistique)
- évaluer les performances (matrice de confusion, courbe ROC, AUC)

***

## 2. 📥 Importation des librairies

Les librairies principales utilisées sont :

- **pandas, numpy** : manipulation et préparation de données
- **matplotlib, seaborn** : visualisation statistique et graphiques exploratoires
- **scikit-learn (sklearn)** :
    - prétraitement et sélection de variables
    - imputation des valeurs manquantes
    - modèles de régression linéaire et logistique
    - métriques d’évaluation (R², MAE, RMSE, confusion_matrix, roc_curve, roc_auc_score)
- **TSNE, Isomap** : réduction dimensionnelle et visualisation en 2D
- **pickle** : sauvegarde des modèles entraînés
- **warnings** : filtrage des messages d’alerte pour un affichage plus lisible

***

## 3. 📂 Chargement du dataset

Le fichier est récupéré depuis **Kaggle** via `kagglehub` :

```python
path = kagglehub.dataset_download("miguelroca/global-health-nutrition-mortality-economic-data")
df = pd.read_csv(os.path.join(path, csv_file))
```

Une première inspection est réalisée pour comprendre la structure des données :

```python
df.info()
df.describe()
```

Cette étape permet de vérifier :

- le nombre de lignes et de colonnes
- les types de chaque variable
- la présence de valeurs manquantes et d’éventuelles incohérences

***

## 4. 🔎 Analyse exploratoire (EDA)

### 4.1 📈 Distribution de l’Espérance de Vie

Plusieurs visualisations sont utilisées pour analyser la variable **Life Expectancy** :

- Histogramme global de l’espérance de vie
- Densités de distribution pour différents millésimes (ex. 1990, 2000, 2010, 2019)
- Boxplots par année

Exemples typiques de code :

```python
sns.histplot(df['Life Expectancy'])
sns.distplot(df['Life Expectancy'][df['Year'] == 1990])
plt.figure(figsize=(15, 5))
sns.boxplot(x='Year', y='Life Expectancy', data=df)
```

Ces graphiques permettent de visualiser la dispersion, les asymétries et l’évolution de l’espérance de vie dans le temps.

### 4.2 🚻 Comparaison par genre

Une comparaison par **genre** est réalisée :

```python
sns.pairplot(df[['Year', 'Gender', 'Life Expectancy']], hue='Gender')
```

Cette analyse met en évidence d’éventuelles différences d’espérance de vie entre les sexes selon les années.

### 4.3 ⚠️ Détection des valeurs aberrantes (IQR)

Les valeurs aberrantes de l’espérance de vie sont détectées via l’**intervalle interquartile (IQR)** :

```python
outliers_LifeExpectancy = find_outliers_IQR(df['Life Expectancy'])
sns.distplot(outliers_LifeExpectancy)
```

Cela permet de repérer des pays/années extrêmes (très faible ou très forte espérance de vie).

***

## 5. 🧹 Analyse \& gestion des valeurs manquantes

Trois fonctions spécifiques sont définies pour diagnostiquer les **NaNs** :

1. 🔢 **Nombre de NaNs par ligne**

```python
def nans_per_row(df):
    ...
```

2. 🌐 **Nombre de NaNs par pays**

```python
def nans_per_country(df):
    ...
```

3. 📊 **Histogramme des lignes contenant des NaNs**

```python
def histNaNs(df):
    ...
```


Ces outils permettent :

- d’identifier les pays ou les années les plus incomplets
- de décider quelles observations ou variables conserver, imputer ou supprimer avant la modélisation

***

## 6. 🌍 Visualisation par pays

La fonction `countriesGraph` trace l’évolution d’une variable donnée pour un ou plusieurs pays. Exemple :

```python
countriesGraph(df, ['Morocco', 'France'], 'Life Expectancy')
```

Applications typiques :

- comparaison de trajectoires d’espérance de vie entre pays
- analyse visuelle de l’impact de politiques de santé ou de crises sur une période donnée

***

## 7. 📈 Modélisation Machine Learning

### 7.1 📉 Régression Linéaire

La **régression linéaire** est utilisée pour prédire une variable continue, en particulier l’espérance de vie.

Schéma général :

```python
from sklearn.linear_model import LinearRegression

model_lin = LinearRegression()
model_lin.fit(X_train, y_train)
y_pred = model_lin.predict(X_test)
```

Les principales métriques possibles sont :

- **R²** (coefficient de détermination)
- **MAE** (Mean Absolute Error)
- **RMSE** (Root Mean Squared Error)

Ces indicateurs mesurent la qualité de la prédiction et l’erreur moyenne entre valeurs réelles et prédites.

### 7.2 🧮 Régression Logistique

La **régression logistique** sert pour des tâches de **classification binaire**, par exemple :

> 1 = « espérance de vie élevée », 0 = « espérance de vie faible »

Schéma général :

```python
from sklearn.linear_model import LogisticRegression

model_logistic = LogisticRegression()
model_logistic.fit(X_train_logistic, y_train_logistic)
y_pred_logistic = model_logistic.predict(X_test_logistic)
y_pred_proba_logistic = model_logistic.predict_proba(X_test_logistic)[:, 1]
```

Cette modélisation permet de transformer un problème de santé continue en décisionnel (haut/bas, risque élevé/faible).

***

## 8. 🧪 Évaluation du modèle de régression logistique

### 8.1 🧱 Matrice de confusion

La **matrice de confusion** évalue la qualité de la classification :

```python
cm = confusion_matrix(y_test_logistic, y_pred_logistic)
sns.heatmap(cm, annot=True, fmt='d', cmap='Blues')
```

Elle met en évidence :

- **VP** : Vrais Positifs
- **VN** : Vrais Négatifs
- **FP** : Faux Positifs
- **FN** : Faux Négatifs

Ces éléments servent à calculer précision, rappel, F1-score, etc.

### 8.2 📉 Courbe ROC \& AUC

La **courbe ROC** et l’**AUC** (Area Under the Curve) mesurent la capacité du modèle à bien séparer les deux classes.

```python
fpr, tpr, thresholds = roc_curve(y_test_logistic, y_pred_proba_logistic)
auc = roc_auc_score(y_test_logistic, y_pred_proba_logistic)
```

Interprétations :

- comparaison au classifieur aléatoire (diagonale ROC)
- plus l’AUC est proche de 1, meilleure est la capacité de séparation du modèle

***

## 9. 🗺️ Réduction dimensionnelle

Deux techniques de **réduction dimensionnelle** sont disponibles :

- **t-SNE (TSNE)**
- **Isomap**

Elles permettent de projeter les données de grande dimension en 2D pour :

- révéler des **clusters naturels** entre pays
- repérer des **similarités de profils** (santé, nutrition, économie)
- visualiser la **structure cachée** du dataset

Ces méthodes sont utiles pour l’exploration visuelle et pour préparer d’éventuels clustering ou analyses avancées.

***

## 10. 💾 Sauvegarde des modèles

La sauvegarde des modèles se fait via le module **pickle** :

```python
pickle.dump(model_logistic, open('model_logistic.pkl', 'wb'))
pickle.dump(model_lin, open('model_lin.pkl', 'wb'))
```

Objectifs :

- réutiliser les modèles sans avoir à les réentraîner
- déployer les modèles dans d’autres scripts ou applications (API, dashboards, etc.)

***

## 11. 🏁 Conclusion générale

L’exploration complète du dataset **Global Health, Nutrition, Mortality, Economic Data** a permis :

- ✅ Une compréhension approfondie des **tendances mondiales**
    - évolution de l’espérance de vie
    - différences par genre et par pays
    - détection de valeurs extrêmes et anomalies
- ✅ Une **préparation robuste** du dataset
    - diagnostic et gestion des NaNs
    - traitement des valeurs aberrantes
    - visualisations adaptées par pays, année et genre
- ✅ La **construction de modèles de Machine Learning**
    - régression linéaire pour la prédiction continue de l’espérance de vie
    - régression logistique pour la classification (espérance de vie élevée vs faible)
- ✅ Une **évaluation rigoureuse**
    - matrice de confusion pour la classification
    - courbe ROC et AUC pour la performance globale du classificateur

Ce pipeline constitue une base complète et cohérente pour un projet de Data Science appliqué à la **santé mondiale**, offrant à la fois des outils descriptifs, prédictifs et décisionnels.
<span style="display:none">[^1][^2][^3][^4][^5][^6]</span>

<div align="center">⁂</div>

[^1]: https://www.kaggle.com/datasets/miguelroca/global-health-nutrition-mortality-economic-data

[^2]: https://www.nature.com/articles/s41598-025-20345-7

[^3]: https://github.com/jnliou/globalhealthdata

[^4]: https://www.sciencedirect.com/science/article/abs/pii/S0010482521004005

[^5]: https://pmc.ncbi.nlm.nih.gov/articles/PMC12100565/

[^6]: https://ourworldindata.org/health-meta

